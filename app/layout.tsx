import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "H.V. Ranade & Associates | Insurance Solutions",
  description: "35+ years of protecting what matters. Life, Health, Vehicle, Home, Fire, Marine, Engineering, Liability & Industrial Insurance.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
